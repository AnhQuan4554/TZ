export enum EnumPanelWarning {
  none = 'NO WARNINGS',
  temperature = 'TEMPERATURE NOT OPTIMAL',
  broken = 'PANELS OFFLINE',
  offline = 'OFFLINE',
}

export enum EnumGenerationLevel {
  high = 'High',
  medium = 'Medium',
  low = 'Low',
  none = 'N/A',
}
